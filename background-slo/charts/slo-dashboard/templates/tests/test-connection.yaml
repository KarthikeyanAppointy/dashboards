apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "slo-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "slo-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "slo-dashboard.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
